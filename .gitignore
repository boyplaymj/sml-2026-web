# OS junk
.DS_Store
Thumbs.db
desktop.ini

# 原始美術檔(太大,不放 git)
*.psd

# 本機預覽設定(機器相關)
.claude/launch.json

# Claude 本機設定
.claude/settings.local.json

# 執行中程序的 pid 檔(機器相關,不入庫)
*.pid

# bridge 編譯產物(由原始碼建置,不入庫)
aws/discord-bridge/sml-discord-bridge
aws/codex-bridge/sml-codex-bridge
__pycache__/
